# PrePack v1 — แผนงานที่ตกลงกันแล้ว (สรุปจาก session 23 ก.ย. 2569)

> **สถานะ:** implement แล้วใน `src/PrePack` (ข้อ 9 ขั้น 1–6) — ยังไม่ได้ทดสอบกับ MySQL/INVS/เครื่องพิมพ์จริง (ดู devlog)
> โค้ด Go + Gin ที่ root (v0.1) เป็นต้นแบบรุ่นแรก ใช้อ้างอิงได้ แต่ไม่ต้องพัฒนาต่อ

## 1. ปัญหาที่ต้องแก้ (จากหน้างานห้องยา)

- งานแบ่งบรรจุมี 3 แบบ:
  - ยาเม็ดจากกระปุก 1000 เม็ด แบ่งเป็นซอง 30 / 60 / 100 … เม็ด (pre-pack)
  - ยาเม็ดแบ่งเป็นซอง unit dose ซองละ 1–5 เม็ด
  - ครีมจากกระปุก 500 g แบ่งเป็น 5 g
- ปัญหาเดิม:
  - ไม่มีการบันทึกภาระงาน
  - ปริมาณงานของแต่ละคนไม่เท่ากัน
  - รูปแบบการแพ็คไม่เป็นมาตรฐาน
- ตอนนี้พิมพ์ฉลากจาก Excel ลงสติกเกอร์ 8.5 × 5 ซม. แบ่ง 3×3 ดวง มีส่วนหัวที่ฉีกทิ้ง
- ข้อมูลบนฉลาก: ชื่อยา, จำนวน (#), Lot, ผู้บรรจุ, วันบรรจุ (Mfd), วันหมดอายุ (Exp) — template ดูข้อ 4

## 2. เป้าหมาย

คนแพ็คยากรอกข้อมูลให้น้อยที่สุด เลือกแค่ **ผู้บรรจุ → ยา → จำนวนต่อซอง** แล้วกด **พิมพ์** ฉลากออกทันทีแบบ silent print และระบบบันทึกภาระงานให้เอง

## 3. หน้าจอ (หน้าเดียว)

```
┌──────────────────────────────────────────────────────────────┐
│ [ผู้บรรจุ ▼] [ยา: ค้นหา INVS…] [Lot ▼] [EXP] [#จำนวน] (เม็ด)  ⚙ │  header
├──────────────────────────────────────────────────────────────┤
│              ตัวอย่างสติกเกอร์ที่จะพิมพ์ (ขนาดเหมือนจริง)            │  body
├──────────────────────────────────────────────────────────────┤
│   จำนวนหน้า [ − 2 + ]   จำนวนดวง [18]                [ 🖨 พิมพ์ ] │  footer
└──────────────────────────────────────────────────────────────┘
```

- **ผู้บรรจุ** — จำคนล่าสุดไว้
- **ยา** — พิมพ์ค้นหาจาก INVS แล้วเติม Lot/EXP ให้อัตโนมัติ (Lot ที่มีของและหมดอายุก่อน)
  แก้ไขได้ทุกช่อง หรือกรอกเองทั้งหมดได้ (ยา Manual)
- **จำนวน** = **จำนวนต่อซอง** (#30 เม็ด, #5 g) มีปุ่มลัดเป็นขนาดที่ยานั้นเคยแพ็คบ่อย
- **ประเภทยา** — ระบบรู้เอง (ข้อ 5) แสดงเป็นป้ายเล็ก ๆ ไม่ต้องกด
- **วันบรรจุ = วันนี้เสมอ** (วันที่ของเครื่อง ณ ตอนกดพิมพ์) ไม่มีช่องให้กรอก
- **Footer** — จำนวนหน้า และจำนวนดวงที่พิมพ์ (แก้ได้) พร้อมปุ่มพิมพ์
- **⚙ (มุมขวาบน)** — เจ้าหน้าที่, ฐานข้อมูล (MySQL / INVS), เครื่องพิมพ์, ตั้งค่าสติกเกอร์,
  อายุยาตามประเภท, ตารางหน่วย → ประเภท, ประวัติ, รายงานภาระงาน

## 4. สติกเกอร์ (ตั้งค่าใน ⚙)

- ขนาดแผ่น (ค่าเริ่มต้น 85 × 50 มม.) และความสูง**ส่วนหัว**กับ**ส่วนท้าย**ที่ฉีกทิ้ง
- ความสูง**แต่ละแถว** และความกว้าง**แต่ละคอลัมน์** ใส่ได้หลายค่า (เช่น `12, 13, 13`) — เว้นว่าง = แบ่งเท่ากัน
  (ผลรวมต้องไม่เกินที่ว่าง: แถว = แผ่น − หัว − ท้าย − ช่องว่าง, คอลัมน์ = แผ่น − ช่องว่าง)
- จำนวนดวง แถว × คอลัมน์ เช่น 3×3, 3×4, 4×4
- offset ซ้าย/ขวา และบน/ล่าง
- ช่องว่างระหว่างดวง แนวตั้ง / แนวนอน
- ขนาดตัวอักษร, ปี พ.ศ./ค.ศ.
- ใช้วิธีวาง layout เป็นมิลลิเมตรต่อจาก v0.1 (`web/common.js` `buildFrames`, `internal/domain/label.go`)

**Template ข้อความในแต่ละดวง (ตกลงแล้ว)**

```
ยา: Paracetamol 500 mg #30
Lot: A12345 ผู้บรรจุ:สมชาย
Mfd: 23/09/69
Exp: 22/09/70
```

ชิดซ้ายทุกบรรทัด

- บรรทัด 1: `ยา:` ชื่อบนฉลาก ตามด้วย `#จำนวน` ต่อท้ายทันที **ไม่มีหน่วย**
  ชื่อยาวเกินให้**ตัดคำ** (…) โดย `#จำนวน` ต้องเห็นครบเสมอ
- บรรทัด 2: `Lot:` lot (แสดงครบ) ตามด้วย `ผู้บรรจุ:ชื่อ` — ยาวเกินให้**ตัดคำ** (…)
  (Lot ≤ 7 ตัวชื่อทั้ง 21 คนแสดงครบ; Lot ยาว ~10 ตัว ชื่อยาวจะถูกตัด)

**ชื่อบนฉลาก (ตกลงแล้ว):** ช่อง "ชื่อบนฉลาก" ข้างช่องยา — ค่าเริ่มต้น = ชื่อเต็ม; ผู้ใช้ย่อเองได้ แล้ว**จำไว้ต่อยา**
(key = รหัส INVS หรือ hash ของชื่อยา Manual) บันทึกลง server (ตาราง `drug_labels`, migration 003) ใช้ได้ทุกเครื่อง
local เป็น fallback; ว่าง/เท่าชื่อเต็ม = ใช้ชื่อเต็ม; ประวัติ/รายงานยังเก็บชื่อเต็ม
- `Mfd:` = วันบรรจุ (วันนี้)
- `Exp:` = วันหมดอายุบนฉลาก (ข้อ 5)
- รูปแบบวันที่ `dd/mm/yy` ตามตัวเลือก พ.ศ./ค.ศ. ใน ⚙

## 5. ประเภทยาและวันหมดอายุบนฉลาก

**ลำดับการตัดสินประเภทยา**

1. **หน่วยจาก INVS** (`DRUG_GN`) เทียบกับตารางหน่วย → ประเภท (อยู่ใน seed และแก้ได้ใน ⚙)

   | หน่วย | ประเภท |
   |---|---|
   | TAB, CAP, เม็ด, แคปซูล | ยาเม็ด |
   | TUBE, หลอด, กรัม, g, JAR, กระปุก | ครีม |
   | BOT, ขวด, ml, ซีซี | ยาน้ำ |

2. **ชื่อยา** ใช้ตัดสินเมื่อหน่วยกำกวม (ชื่อยาชนะ)
   - `tab, cap` → เม็ด
   - `cream, oint, gel, lotion` → ครีม
   - `syr, susp, sol, elixir, mixt` → น้ำ
3. **ยา Manual** ดูจากชื่อยาอย่างเดียว

หน่วย (เม็ด / g / ml) ใช้แสดงบนหน้าจอและเก็บใน log เท่านั้น — **บนฉลากพิมพ์แค่ `#30` ไม่มีหน่วย**

**วันหมดอายุบนฉลาก**

- สูตร: `min(วันบรรจุ + อายุตามประเภท, EXP เดิมจาก INVS)` และแก้เองในช่อง EXP ได้
- ค่าเริ่มต้นใน seed (แก้ได้ใน ⚙):
  - ยาเม็ด 365 วัน
  - ครีม 180 วัน
  - ยาน้ำ 30 วัน

> ⏳ **ยังไม่รู้ชื่อคอลัมน์หน่วยใน `DRUG_GN`** ต้องรันใน INVS:
> ```sql
> SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'DRUG_GN'
> ```
> ระหว่างนี้ให้โปรแกรม auto-detect คอลัมน์ที่มีคำว่า UNIT / DOSAGE / FORM และให้เลือกคอลัมน์ใน ⚙ ได้

## 6. การบันทึก (log ทุกครั้งที่พิมพ์ → รายงานภาระงาน)

| ฟิลด์ | หมายเหตุ |
|---|---|
| เจ้าหน้าที่ | ผู้บรรจุ |
| ชื่อยา, working_code | working_code เป็น NULL ถ้าเป็นยา Manual |
| แหล่งข้อมูล | `INVS` หรือ `MANUAL` (แยกนับได้) |
| ประเภทยา, หน่วย, จำนวนต่อซอง | |
| Lot, วันบรรจุ, EXP (EXP เดิม + EXP บนฉลาก) | |
| จำนวนหน้า, จำนวนดวงที่พิมพ์ | |
| เวลาพิมพ์, ชื่อเครื่อง | |

รายงานภาระงาน: นับรายการยา, จำนวนหน้า และจำนวนดวงต่อคน แยก INVS/Manual และแยกประเภทยา ตามช่วงวันที่ export ได้

**หน้ารายงาน (ตกลงจาก mockup):** ⚙ → รายงานภาระงาน
- ตัวกรอง: ช่วงวันที่ + ปุ่มลัด วันนี้ / สัปดาห์นี้ / เดือนนี้, แหล่ง (ทั้งหมด / INVS / Manual), Export CSV (UTF-8 BOM เปิดใน Excel ได้)
- ตัวเลขสรุป 4 ช่อง: รายการที่พิมพ์, หน้า, ดวง, % ยา Manual
- ตารางรายคน: รายการ, หน้า, ดวงแยก เม็ด/ครีม/น้ำ, รวมดวง, **แต้มงาน, % ภาระงาน** + แถบ — คลิกชื่อดูรายการของคนนั้น
- ⚙ → ประวัติการพิมพ์: ค้นหาตาม Lot (recall) + Export CSV
- ไม่มีขั้นตอนเภสัชกรตรวจ (ค่าที่เลือกเอง ผู้ใช้ยังไม่ได้ตอบ)

**แต้มภาระงาน (ตกลงแล้ว):** แต้ม = จำนวนดวง × factor
- factor ขึ้นกับประเภทยา + ช่วง #จำนวนต่อซอง เช่น ยาเม็ด #1–5 / #6–30 / #31–60 / #61–100 / #101 ขึ้นไป, ครีม, ยาน้ำ
- ค่าเริ่มต้น factor = 1 ทุกช่วง (ซองใหญ่กับซองเล็กนับเท่ากัน) จนกว่าจะตั้งใหม่
- ตั้งที่ ⚙ → แต้มภาระงาน **ต้องใส่รหัส** (C# ตรวจอีกชั้นตอนบันทึก, เก็บเป็น SHA-256 ในโค้ด ไม่ใช่ security boundary)
- เก็บใน MySQL ตาราง `work_factors` ใช้ร่วมทุกเครื่อง; `print_logs` เก็บ snapshot `work_factor`, `work_points` → แก้ factor ไม่กระทบประวัติ
- % ภาระงาน = แต้มของคนนั้น ÷ แต้มรวมทุกคนในช่วงวันที่/แหล่งที่เลือก

**ใช้ข้อมูลจาก server ก่อน (ตกลงแล้ว) — local.db เป็น fallback + backup:**
- ถ้าตั้ง MySQL แล้วและเชื่อมได้: เปิดโปรแกรม / เปิดรายชื่อ / บันทึกเจ้าหน้าที่ / แก้ factor / พิมพ์ → sync กับ server ทันทีแล้วใช้ข้อมูลนั้น
  (ใช้หลายเครื่องพร้อมกันได้ — ทุก log มี `machine_name` + `client_uid` ไม่ปนกัน)
- ถ้ายังไม่ตั้ง หรือ server ไม่ตอบ (ภายใน 1 นาทีที่ผ่านมา) → ใช้ local.db ทันที แล้วส่งขึ้นทีหลัง
- ลำดับตอนพิมพ์: พิมพ์ → เขียน local → ส่ง server (สติกเกอร์ออกก่อนแตะเครือข่ายเสมอ)
- **seed เจ้าหน้าที่ 21 คน** ใน `seed-config.json` → ใส่ local.db ตอนเปิดโปรแกรม (ถ้ายังไม่มี) uid คำนวณจากชื่อ
  ทุกเครื่องจึงได้ uid เดียวกัน ไม่ซ้ำบน server; timestamp = epoch → ถ้าใครแก้บน server แล้ว ของ server ชนะ

**กลไกบันทึกในเครื่อง + sync:**
- ทุกเครื่องมีฐานข้อมูล SQLite ของตัวเอง `%APPDATA%\PrePack\prepack-local.db` — เจ้าหน้าที่, log การพิมพ์, ตาราง factor
- พิมพ์ / เพิ่มเจ้าหน้าที่ / แก้ factor เขียนลง local ทันที **ไม่ต้องมี MySQL** (ใช้งานได้ตั้งแต่ยังไม่ตั้งค่าฐานข้อมูล)
- sync เบื้องหลังทุก 2 นาที + หลังพิมพ์ + ปุ่ม "Sync ตอนนี้" (ล้มเหลวแล้วรอ 60 วินาทีก่อนลองใหม่ ไม่ทำให้จอค้าง)
  - ลำดับ: สร้าง schema (มี guard HOSxP/JHCIS) → เจ้าหน้าที่ สองทาง (key = uid, ใครแก้ทีหลังชนะ) → ตาราง factor (version ใหม่กว่าชนะ)
    → log ส่งขึ้นอย่างเดียว (กันซ้ำด้วย `client_uid`)
- ส่งขึ้นแล้ว **ไม่ลบในเครื่อง** — เป็น backup; ปุ่ม "ส่งข้อมูลในเครื่องขึ้นใหม่ทั้งหมด (กู้คืน)" ใช้ตอน server ถูกล้าง
- รายงาน/ประวัติ: ใช้ MySQL (รวมทุกเครื่อง) ถ้าเชื่อมได้ ไม่งั้นใช้ข้อมูลในเครื่องพร้อมแถบเตือน "ข้อมูลจากเครื่องนี้เท่านั้น"
- ข้อจำกัด: 2 เครื่องเพิ่มชื่อคนเดียวกันตอน offline → ได้ 2 รายชื่อ; ไฟล์ local ไม่เข้ารหัส (ไม่มีข้อมูลผู้ป่วย)

## 7. สถาปัตยกรรม (ตกลงแล้ว)

- **C# .NET 10 (LTS) WinForms + WebView2** — เลือก .NET 10 เพราะ .NET 8/9 หมด support 10 พ.ย. 2569
  (BoxBox ยังเป็น net9.0; เครื่อง dev ต้องติดตั้ง .NET 10 SDK)
- **exe ไฟล์เดียว** (ยืนยันแล้ว แม้ BoxBox จะเลือก installer): `PublishSingleFile` + `SelfContained` + win-x64
  - `IncludeNativeLibrariesForSelfExtract=true` (WebView2Loader.dll อยู่ใน exe)
  - `EnableCompressionInSingleFile=false` เพื่อลดโอกาส antivirus แจ้งผิด (BoxBox เคยเจอ)
  - หน้าเว็บ (HTML/CSS/JS) ฝังเป็น `EmbeddedResource` — ฟอนต์ใช้ **Leelawadee UI** ที่มากับ Windows (ยังไม่ฝัง Sarabun
    เพราะต้องดาวน์โหลดไฟล์ฟอนต์ก่อน ถ้าต้องการ Sarabun ให้วาง .ttf ใน `wwwroot/fonts/` แล้วเพิ่ม @font-face)
  - MySQL client ใช้ **MySqlConnector** (แทน MySql.Data ของ BoxBox — รองรับ MySQL 5.7 / MariaDB 10.3 ดีกว่า)
  - exe ที่ publish ขนาด ~118 MB (รวม runtime .NET, ไม่บีบอัด)
    เสิร์ฟผ่าน `WebResourceRequested` ที่ `https://prepack.app/` — ไม่แตกไฟล์ ไม่พึ่งอินเทอร์เน็ต
  - WebView2 Runtime ใช้ของ Windows 11 ที่มีอยู่แล้ว (Evergreen)
- **ไม่ใช้ Gin หรือ web server** หน้าจอคุยกับ C# ผ่าน COM bridge
  (`window.chrome.webview.hostObjects.bridge`) แบบ BoxBox — parameter เป็น string/int/bool, ข้อมูลซับซ้อนส่ง JSON string
- **Silent print:** `CoreWebView2.PrintAsync(CoreWebView2PrintSettings)`
  (ต่างจาก BoxBox ที่วาดด้วย GDI+ `PrintDocument` — ใช้ HTML เพื่อให้ตรงกับ preview และตัดคำไทยได้ดี)
  - ระบุ `PrinterName`, `PageWidth`/`PageHeight` ตามสติกเกอร์, margin 0
  - ปิด header/footer ของเบราว์เซอร์
  - พิมพ์ HTML ชุดเดียวกับตัวอย่างบนจอ ภาษาไทยถูกต้อง
- **ใช้โค้ดจาก BoxBox ซ้ำ** (`D:\COACH\source\repo\BoxBox\Bridge\WebBridge.cs`):
  - `ReadInvsIni()` / `ParseInvsIni()` — หา `invs.ini` อัตโนมัติทุก fixed drive แล้วถอด Base64 ใน section `[Pharms]` (ServerName, Port, Database, User, Password)
  - `BrowseInvsIni()` — ให้ผู้ใช้เลือกไฟล์เอง
  - `GetPrinters()` — รายชื่อเครื่องพิมพ์
  - Microsoft.Data.SqlClient สำหรับ INVS, MySql.Data สำหรับ MySQL
  - auto-update (`CheckForUpdate` / `DownloadVerifiedAsync` + SHA-256 + host allowlist) — ต้องดัดแปลง
    เพราะ BoxBox อัปเดตผ่าน installer แต่ PrePack เป็น exe เดียว (ดาวน์โหลด exe ใหม่ → สลับไฟล์ตอนปิดโปรแกรม)
- **ไม่ลอกแบบ BoxBox:** `QueryInvs` ของ BoxBox รับ SQL ดิบจาก JS และส่งรหัสผ่านจาก JS ทุกครั้ง
  → PrePack ให้ C# ถือ connection config เอง และมี method เฉพาะงาน (`SearchDrugs(kw)`, `GetLots(code)`)
  ใช้ parameter ทั้งหมด JS ส่ง SQL ไม่ได้
- **ติดตั้งบนเครื่องห้องยา** แล้วต่อ MySQL server ของโรงพยาบาล
  **โปรแกรมต้องสร้าง database และตารางเอง** (มีแค่ host / user / password)
- **DB `prepack` แยกจาก HOSxP / JHCIS** (ยืนยันแล้ว) — เขียนได้เฉพาะ DB ของ PrePack เอง
  ห้ามเขียนลง HOSxP / JHCIS / INVS (อ่านอย่างเดียว ตามกฎใน `D:\COACH\source\repo\CLAUDE.md`)

### คำสั่ง SQL ของ INVS (SQL Server, อ่านอย่างเดียว, ใช้ parameter ห้ามต่อ string)

```sql
-- ค้นหายา
SELECT TOP 20 RTRIM(WORKING_CODE) AS WORKING_CODE, RTRIM(DRUG_NAME) AS DRUG_NAME,
       RTRIM(ISNULL(DRUG_NAME_TH,'')) AS DRUG_NAME_TH /*, <คอลัมน์หน่วย> */
FROM DRUG_GN
WHERE DRUG_NAME LIKE @kw OR DRUG_NAME_TH LIKE @kw OR WORKING_CODE = @code
ORDER BY DRUG_NAME;

-- Lot ที่มีของ เรียงตามวันหมดอายุก่อน
-- EXPIRED_DATE เป็นข้อความรูปแบบ 'YYYYMMDD' (ดู _invsDateToIso ใน BoxBox FillModal.js)
SELECT RTRIM(LOT_NO) AS LOT_NO, EXPIRED_DATE, SUM(QTY_ON_HAND) AS QTY
FROM INV_MD_C
WHERE WORKING_CODE = @code AND QTY_ON_HAND > 0 AND EXPIRED_DATE >= @today
GROUP BY LOT_NO, EXPIRED_DATE
ORDER BY EXPIRED_DATE;
```

## 8. Config แบ่ง 3 ระดับ

| อยู่ที่ไหน | เก็บอะไร |
|---|---|
| **ฝังใน exe** (`seed-config.json`) | ค่าเริ่มต้นทั้งหมด: layout 85×50 แบบ 3×3, อายุยาตามประเภท, ตารางหน่วย → ประเภท, คำในชื่อยา |
| **`%APPDATA%\PrePack\config.json`** | ชื่อเครื่องพิมพ์, layout/offset ของเครื่องนี้, การเชื่อมต่อ MySQL และ INVS (รหัสผ่านเข้ารหัสด้วย DPAPI) |
| **`%APPDATA%\PrePack\prepack-local.db`** (SQLite) | ที่บันทึกหลักของเครื่องนี้ + backup: เจ้าหน้าที่, log การพิมพ์, factor |
| **MySQL** | ฐานกลางรวมทุกเครื่อง (sync จาก local): เจ้าหน้าที่, log การพิมพ์, factor |

**เปิดครั้งแรก:**
1. คัดลอก seed ไปไว้ที่ AppData
2. หา `invs.ini` อัตโนมัติ
3. อ่าน env `PREPACK_DB_HOST` / `PREPACK_DB_PORT` / `PREPACK_DB_USER` / `PREPACK_DB_PASS` ถ้ามี
4. `CREATE DATABASE IF NOT EXISTS` แล้วรัน migration

## 9. ลำดับงานที่เสนอ

1. วางโครงโปรเจกต์ C# (.csproj net10.0-windows single-file, WebView2, bridge) และ seed config + AppData loader
2. ส่วน MySQL: สร้าง DB, migration, เจ้าหน้าที่, log การพิมพ์
3. ส่วน INVS: หา `invs.ini`, ค้นหายา, ดึง Lot, **หาคอลัมน์หน่วย**, ตัดสินประเภทยา
4. หน้าจอหลัก (header / preview / footer) และหน้า ⚙
5. Silent print ด้วย `PrintAsync` แล้วปรับ offset กับเครื่องพิมพ์จริง
6. รายงานภาระงาน, export
7. Build exe และทดสอบบนเครื่องห้องยา
